# 개인 산출물: 선착순 쿠폰 발급 동시성 제어 개선 기록

## 1. 산출물 주제

**대량 동시 요청 환경에서 쿠폰 발급 수량 정합성과 중복 발급을 보장하기 위한 DB 기반 동시성 제어 적용**

---

## 2. 문제 정의

선착순 쿠폰 발급 API는 단순히 쿠폰을 조회하고 발급 이력을 저장하는 CRUD 기능이 아니다. 동시에 많은 사용자가 같은 쿠폰을 요청할 수 있기 때문에 다음 정합성을 반드시 보장해야 한다.

1. 전체 발급 수량은 쿠폰 재고를 초과하면 안 된다.
2. 동일 사용자는 같은 쿠폰을 중복 발급받으면 안 된다.
3. 발급 수량 증가와 발급 이력 저장은 하나의 일관된 흐름으로 처리되어야 한다.

이번 작업에서는 동시성 테스트를 통해 문제를 재현하고, DB의 atomic update, unique constraint, pessimistic lock을 단계적으로 적용해 정합성을 개선했다.

---

## 3. 발생한 문제

## 3.1 문제 1: 발급 수량 초과

초기 방식에서는 쿠폰을 조회한 뒤 현재 발급 수량을 확인하고, 발급 가능하면 수량을 증가시키는 흐름이었다.

```text
쿠폰 조회
→ issuedQuantity 확인
→ totalQuantity와 비교
→ 발급 이력 저장
→ issuedQuantity 증가
```

하지만 동시에 많은 요청이 들어오면 여러 트랜잭션이 같은 `issuedQuantity` 값을 읽고 각각 발급을 진행할 수 있다.

예상 결과는 다음과 같다.

```text
totalQuantity = 100
동시 요청 수 = 1,000
발급 성공 수 = 100
DB 저장 수 = 100
```

하지만 동시성 제어가 없으면 실제 발급 수량이 재고보다 많아질 수 있다.

---

## 3.2 문제 2: 동일 사용자 중복 발급

동일 사용자가 같은 쿠폰을 여러 번 동시에 요청하는 경우도 문제가 된다.

애플리케이션에서 다음과 같이 중복 여부를 먼저 확인하더라도 안전하지 않다.

```java
issuedCouponRepository.existsByCouponIdAndUserId(couponId, userId)
```

동시 요청에서는 다음 경쟁 조건이 발생할 수 있다.

```text
Thread A: 아직 발급 이력 없음 확인
Thread B: 아직 발급 이력 없음 확인
Thread A: 발급 이력 저장
Thread B: 발급 이력 저장
```

결과적으로 같은 사용자가 같은 쿠폰을 여러 번 발급받을 수 있다.

---

## 3.3 문제 3: 발급 수량 증가와 발급 이력 저장의 불일치

수량 증가를 DB에서 원자적으로 처리하더라도, 발급 이력 저장과 완전히 묶이지 않으면 다른 정합성 문제가 생긴다.

예를 들어 다음 흐름에서는 먼저 쿠폰 발급 수량을 증가시킨 뒤 발급 이력을 저장한다.

```java
int updated = couponRepository.tryIncreaseIssueQuantity(couponId, CouponStatus.ACTIVE);

if (updated == 0) {
    throw new CouponExhaustedException();
}

IssuedCoupon issuedCoupon = IssuedCoupon.issue(coupon, request.getUserId());
try {
    IssuedCoupon saved = issuedCouponRepository.saveAndFlush(issuedCoupon);
    return IssuedCouponResponse.from(saved);
} catch (DataIntegrityViolationException e) {
    throw new DuplicateCouponIssueException();
}
```

이 방식은 `issuedQuantity` 증가는 성공했지만, 이후 `(coupon_id, user_id)` unique constraint 위반으로 발급 이력 저장이 실패할 수 있다.

그 경우 실제 발급 쿠폰은 없는데 재고만 감소한 상태가 된다.

```text
발급 수량 증가 성공
→ 중복 발급으로 발급 이력 저장 실패
→ 실제 발급은 안 됐지만 재고는 감소
```

---

## 4. 해결 과정

## 4.1 1차 개선: DB atomic update로 수량 초과 방지

먼저 발급 수량 증가를 애플리케이션에서 처리하지 않고 DB update 쿼리에서 원자적으로 처리했다.

```java
@Modifying
@Query("""
    update Coupon c
    set c.issuedQuantity = c.issuedQuantity +1
    where c.id = :couponId
        and c.status = :activeStatus
        and c.issuedQuantity < c.totalQuantity
    """)
int tryIncreaseIssueQuantity(@Param("couponId") Long couponId,
                             @Param("activeStatus") CouponStatus activeStatus);
```

### 적용 의도

기존 방식은 다음처럼 애플리케이션에서 수량을 확인하고 증가시킨다.

```text
조회 → 비교 → 증가
```

이 흐름은 동시 요청에서 안전하지 않다.

따라서 DB가 직접 조건을 판단하도록 바꿨다.

```text
issuedQuantity < totalQuantity 조건을 만족할 때만 issuedQuantity 증가
```

### 장점

- 수량 확인과 증가가 하나의 update 쿼리에서 처리된다.
- 동시에 많은 요청이 들어와도 `issuedQuantity < totalQuantity` 조건을 DB가 판단한다.
- 재고 초과 발급 가능성을 줄일 수 있다.

### 한계

- 발급 수량 증가와 발급 이력 저장이 분리되어 있다.
- 발급 이력 저장 실패 시 이미 증가한 수량을 되돌리는 문제가 남는다.
- 요청 순서를 보장하지 않는다.

---

## 4.2 2차 개선: DB unique constraint로 중복 발급 방지

동일 사용자 중복 발급은 애플리케이션 검증만으로 막을 수 없다.

따라서 `(coupon_id, user_id)` 조합에 unique constraint를 두어 DB 레벨에서 최종적으로 중복을 방지하도록 설계했다.

```text
issued_coupon
- coupon_id
- user_id

unique(coupon_id, user_id)
```

엔티티 기준으로 표현하면 다음과 같다.

```java
@Entity
@Table(
    uniqueConstraints = {
        @UniqueConstraint(
            name = "uk_issued_coupon_coupon_user",
            columnNames = {"coupon_id", "user_id"}
        )
    }
)
public class IssuedCoupon {
    // ...
}
```

중복 발급이 발생하면 DB가 unique constraint 위반을 발생시키고, 서비스 계층에서는 이를 도메인 예외로 변환한다.

```java
try {
    IssuedCoupon saved = issuedCouponRepository.saveAndFlush(issuedCoupon);
    return IssuedCouponResponse.from(saved);
} catch (DataIntegrityViolationException e) {
    throw new DuplicateCouponIssueException();
}
```

### 적용 의도

동시 요청에서는 여러 스레드가 동시에 중복 검사를 통과할 수 있다.

따라서 중복 여부의 최종 판단은 애플리케이션이 아니라 DB 제약조건이 담당해야 한다.

### 장점

- 같은 사용자가 같은 쿠폰을 여러 번 발급받는 것을 DB가 막는다.
- 애플리케이션 레벨 검증이 실패하더라도 최종 정합성은 유지된다.
- 동시 요청 상황에서 가장 신뢰할 수 있는 중복 방지 방식이다.

### 한계

- unique constraint 위반이 발생하기 전에 수량을 먼저 증가시키면 재고 불일치가 생길 수 있다.
- 따라서 수량 증가와 발급 이력 저장의 트랜잭션 순서가 중요하다.

---

## 4.3 3차 개선: 비관적 락으로 순차 발급 보장

수량 증가와 발급 이력 저장을 하나의 순차적 흐름으로 묶기 위해 쿠폰 row에 비관적 락을 적용했다.

```java
@Lock(LockModeType.PESSIMISTIC_WRITE)
@Query("select c from Coupon c where c.id = :couponId")
Optional<Coupon> findByIdForUpdate(@Param("couponId") Long couponId);
```

서비스 계층에서는 쿠폰을 `for update`로 조회한 뒤, 하나의 트랜잭션 안에서 발급 가능 여부 확인, 발급 이력 저장, 수량 증가를 처리한다.

```java
@Transactional
public void issue(Long couponId, Long userId) {
    Coupon coupon = couponRepository.findByIdForUpdate(couponId)
            .orElseThrow(() -> new IllegalArgumentException("coupon not found"));

    if (coupon.getStatus() != CouponStatus.ACTIVE) {
        throw new IllegalArgumentException("coupon is not active");
    }

    if (coupon.getIssuedQuantity() >= coupon.getTotalQuantity()) {
        throw new IllegalArgumentException("sold out");
    }

    try {
        issuedCouponService.createIssued(couponId, userId);
    } catch (DataIntegrityViolationException e) {
        throw new IllegalArgumentException("already issued");
    }

    coupon.increaseIssuedQuantity();
}
```

### 적용 의도

같은 쿠폰에 대한 발급 요청을 동시에 처리하지 않고 순차 처리한다.

```text
Transaction A: coupon row lock 획득
Transaction B: coupon row lock 대기
Transaction A: 발급 이력 저장 + 수량 증가 + commit
Transaction B: 최신 상태 기준으로 발급 가능 여부 다시 판단
```

### 장점

- 쿠폰별 순차 발급을 보장할 수 있다.
- 발급 가능 여부 확인과 수량 증가가 같은 락 범위 안에서 처리된다.
- 중복 발급 또는 재고 소진 상황에서도 발급 수량 불일치를 줄일 수 있다.

### 한계

- 특정 쿠폰에 요청이 몰리면 모든 요청이 하나의 row lock 앞에서 대기한다.
- 트랜잭션 대기 시간이 길어진다.
- DB connection 점유 시간이 늘어난다.
- 부하가 커지면 connection pool 고갈과 서버 장애로 이어질 수 있다.

---

## 5. 검증 내용

## 5.1 동시 발급 수량 정합성 테스트

### 테스트 목적

1,000명이 동시에 요청해도 재고가 100개라면 정확히 100개만 발급되어야 한다.

### 테스트 조건

```java
int totalQuantity = 100;
int totalRequests = 1_000;
ExecutorService pool = Executors.newFixedThreadPool(50);
CountDownLatch startLatch = new CountDownLatch(1);
```

`CountDownLatch`를 사용해 여러 스레드가 최대한 동시에 발급 요청을 시작하도록 구성했다.

### 검증 기준

```java
assertThat(successCount.get())
        .as("정확히 100명만 성공해야 한다")
        .isEqualTo(totalQuantity);

assertThat(issuedCount)
        .as("DB에 저장된 발급 쿠폰 수는 100개여야 한다")
        .isEqualTo(totalQuantity);

assertThat(failCount.get())
        .as("나머지 900명은 실패해야 한다")
        .isEqualTo(totalRequests - totalQuantity);
```

### 확인한 점

- 성공 요청 수가 쿠폰 재고 수량과 일치해야 한다.
- DB에 저장된 발급 쿠폰 수도 쿠폰 재고 수량과 일치해야 한다.
- 실패 요청 수는 전체 요청 수에서 재고 수량을 뺀 값이어야 한다.

---

## 5.2 동일 사용자 중복 요청 테스트

### 테스트 목적

동일 사용자가 100번 동시에 요청해도 정확히 1번만 발급되어야 한다.

### 테스트 조건

```java
int totalQuantity = 100;
int totalRequests = 100;
Long userId = 1L;
ExecutorService pool = Executors.newFixedThreadPool(50);
CountDownLatch startLatch = new CountDownLatch(1);
```

### 검증 기준

```java
assertThat(successCount.get())
        .as("동일 사용자는 단 1번만 성공해야 한다")
        .isEqualTo(1);

assertThat(issuedCount)
        .as("DB에는 해당 사용자의 쿠폰이 1개만 존재해야 한다")
        .isEqualTo(1);
```

### 확인한 점

- 동일 사용자의 동시 요청 중 하나만 성공해야 한다.
- DB에도 해당 사용자의 발급 이력이 하나만 존재해야 한다.
- 중복 발급은 애플리케이션 체크가 아니라 DB unique constraint로 최종 방어해야 한다.

---

## 6. 현재 코드 기준으로 정리해야 할 지점

현재 코드에는 발급 로직이 두 경로로 나뉘어 있다.

## 6.1 `IssuedCouponService.issueCoupon()`

```java
@Transactional
public IssuedCouponResponse issueCoupon(Long couponId, IssueCouponRequest request) {
    Coupon coupon = couponRepository.findById(couponId)
            .orElseThrow(CouponNotFoundException::new);

    LocalDateTime now = LocalDateTime.now();
    if (now.isBefore(coupon.getStartDate()) || now.isAfter(coupon.getEndDate())) {
        throw new CouponNotAvailableException();
    }

    int updated = couponRepository.tryIncreaseIssueQuantity(couponId, CouponStatus.ACTIVE);
    if (updated == 0) {
        throw new CouponExhaustedException();
    }

    IssuedCoupon issuedCoupon = IssuedCoupon.issue(coupon, request.getUserId());
    try {
        IssuedCoupon saved = issuedCouponRepository.saveAndFlush(issuedCoupon);
        return IssuedCouponResponse.from(saved);
    } catch (DataIntegrityViolationException e) {
        throw new DuplicateCouponIssueException();
    }
}
```

### 특징

- 일반 조회로 쿠폰을 조회한다.
- atomic update로 수량을 먼저 증가시킨다.
- 이후 발급 이력을 저장한다.
- 중복 발급이면 unique constraint 위반을 예외로 변환한다.

### 문제

- 발급 이력 저장이 실패하면 이미 증가한 발급 수량을 되돌리기 어렵다.
- 중복 발급 요청에서 재고만 감소할 수 있다.

---

## 6.2 `CouponService.issue()`

```java
@Transactional
public void issue(Long couponId, Long userId) {
    Coupon coupon = couponRepository.findByIdForUpdate(couponId)
            .orElseThrow(() -> new IllegalArgumentException("coupon not found"));

    if(coupon.getStatus() != CouponStatus.ACTIVE) {
        throw new IllegalArgumentException("coupon is not active");
    }
    if(coupon.getIssuedQuantity() >= coupon.getTotalQuantity()) {
        throw new IllegalArgumentException("sold out");
    }

    try {
        issuedCouponService.createIssued(couponId, userId);
    } catch (DataIntegrityViolationException e){
        throw new IllegalArgumentException("already issued");
    }

    coupon.increaseIssuedQuantity();
}
```

### 특징

- 비관적 락으로 쿠폰 row를 잠근다.
- 쿠폰 상태와 재고를 확인한다.
- 발급 이력을 저장한다.
- 마지막에 발급 수량을 증가시킨다.

### 장점

- 발급 흐름이 하나의 트랜잭션 안에서 처리된다.
- 쿠폰 row lock을 기준으로 순차 발급을 보장할 수 있다.

### 정리 필요 사항

최종적으로 실제 API와 테스트가 어떤 발급 경로를 사용하는지 하나로 정리해야 한다.

현재처럼 발급 로직이 두 곳에 있으면 다음 문제가 생긴다.

- 어떤 로직이 실제 운영 경로인지 불명확하다.
- 테스트가 최종 발급 로직을 검증하지 않을 수 있다.
- 한쪽 로직만 수정되고 다른 쪽 로직은 예전 문제가 남을 수 있다.

따라서 최종 산출물 기준으로는 발급 책임을 하나의 서비스 메서드로 통합하는 것이 좋다.

---

## 7. 부하 테스트에서 발견한 한계

정합성 테스트는 통과했지만, 부하 테스트에서는 서버가 버티지 못하는 문제가 발생했다.

### 원인 분석

비관적 락은 같은 쿠폰에 대한 요청을 순차적으로 처리한다.

선착순 쿠폰처럼 하나의 쿠폰에 요청이 집중되는 경우, 모든 요청이 같은 `coupon` row lock을 기다리게 된다.

```text
동시 요청 증가
→ 같은 coupon row lock 대기
→ 트랜잭션 처리 시간 증가
→ DB connection 점유 시간 증가
→ connection pool 고갈
→ API 응답 지연 또는 timeout
→ 서버 장애
```

즉, 비관적 락은 정합성 문제를 해결했지만 처리량 측면에서는 병목이 된다.

### 정리

| 항목 | 내용 |
|---|---|
| 해결된 문제 | 재고 초과 발급, 동일 사용자 중복 발급 |
| 새로 발견한 문제 | 부하 증가 시 서버 장애 |
| 주요 원인 | 특정 coupon row에 대한 lock 경합 |
| 영향 | DB connection 점유 증가, 응답 지연, timeout |
| 개선 방향 | DB 락 기반 즉시 처리에서 queue 기반 순차 처리로 전환 |

---

## 8. 결론

이번 작업을 통해 다음을 확인했다.

### 8.1 애플리케이션 검증만으로 동시성 정합성을 보장할 수 없다

동시 요청에서는 여러 트랜잭션이 같은 상태를 읽고 동시에 조건을 통과할 수 있다.

따라서 최종 정합성은 DB의 다음 기능으로 보장해야 한다.

- unique constraint
- atomic update
- transaction
- pessimistic lock

---

### 8.2 atomic update는 수량 초과 방지에는 효과적이지만 완전한 발급 트랜잭션은 아니다

`issuedQuantity < totalQuantity` 조건을 DB update에 넣으면 수량 초과 가능성을 줄일 수 있다.

하지만 발급 이력 저장과 하나의 일관된 흐름으로 묶지 않으면, 중간 실패 시 재고와 발급 이력 사이에 불일치가 생길 수 있다.

---

### 8.3 unique constraint는 중복 발급 방지의 최종 방어선이다

동일 사용자 중복 발급은 애플리케이션의 `exists` 체크만으로 막을 수 없다.

`coupon_id + user_id` unique constraint를 통해 DB 레벨에서 중복 데이터를 막아야 한다.

---

### 8.4 pessimistic lock은 정합성을 보장하지만 부하에 약하다

비관적 락은 특정 쿠폰에 대한 발급을 순차 처리하기 때문에 정합성을 강하게 보장한다.

하지만 요청이 집중되면 row lock 병목이 생기고, 부하 테스트에서는 서버 장애로 이어질 수 있다.

---

## 9. 다음 개선 방향

현재 구조는 정합성을 우선하기 위해 DB 비관적 락을 사용했다.

다음 단계에서는 부하를 견디기 위해 발급 요청을 즉시 DB에서 처리하지 않고 queue 기반으로 분리하는 구조를 검토할 수 있다.

```text
API 서버
→ 발급 요청 접수
→ Redis 또는 Kafka에 요청 적재
→ Consumer가 couponId 기준으로 순차 처리
→ DB에 발급 이력 저장
→ 사용자는 발급 결과 조회
```

### 후보 1. Redis atomic counter

Redis에서 쿠폰 재고를 atomic하게 감소시켜 빠르게 선착순 여부를 판단한다.

장점:

- 빠른 처리 가능
- DB row lock 부담 감소

주의점:

- Redis와 DB 사이의 정합성 보상 로직이 필요하다.
- Redis 장애 시 처리 정책이 필요하다.

---

### 후보 2. Kafka 기반 발급 queue

발급 요청을 Kafka topic에 적재하고, `couponId`를 partition key로 사용한다.

```text
partition key = couponId
```

같은 쿠폰에 대한 요청은 같은 partition에서 순서대로 처리되고, 다른 쿠폰은 다른 partition에서 병렬 처리할 수 있다.

장점:

- API 서버와 DB 처리 부하를 분리할 수 있다.
- couponId 기준 순차 처리가 가능하다.
- 트래픽 급증 시 queue가 완충 역할을 한다.

주의점:

- 사용자가 즉시 발급 성공 여부를 받기 어렵다.
- 발급 결과 조회 API 또는 알림 구조가 필요하다.
- consumer 실패 시 retry, DLQ, idempotency 설계가 필요하다.

---

## 10. 프로젝트 적용 산출물

## 10.1 쿠폰 발급 동시성 제어 개선 코드

### 목적

동시 요청 상황에서도 쿠폰 발급 수량과 중복 발급 정합성을 보장한다.

### 구현 범위

- `CouponRepository.findByIdForUpdate`
- `CouponService.issue`
- `(coupon_id, user_id)` unique constraint
- 동시성 테스트 추가

### 완료 기준

- 1,000명 동시 요청 시 발급 수량이 `totalQuantity`를 초과하지 않는다.
- 동일 사용자 100번 동시 요청 시 발급 이력이 1개만 생성된다.
- 성공 수와 DB 저장 수가 일치한다.
- 중복 발급 시 재고가 잘못 감소하지 않는다.

### 면접 질문 포인트

- 왜 애플리케이션 중복 체크만으로는 부족한가?
- unique constraint와 서비스 검증을 둘 다 둔 이유는 무엇인가?
- atomic update와 pessimistic lock의 차이는 무엇인가?
- 비관적 락을 사용하면 어떤 성능 문제가 생기는가?
- 트랜잭션 경계를 어디에 두었고 왜 그렇게 정했는가?

---

## 10.2 동시성 테스트 코드

### 목적

발급 수량 초과와 동일 사용자 중복 발급 문제를 재현하고, 개선 후 정합성을 검증한다.

### 구현 범위

- `CountDownLatch`로 동시에 요청 시작
- `ExecutorService`로 다중 스레드 발급 요청 생성
- 성공 수, 실패 수, DB 저장 수 검증

### 완료 기준

- 재고 100개, 요청 1,000개 → 성공 100, 실패 900
- 동일 사용자 요청 100개 → 성공 1, 저장 1
- 테스트가 반복 실행되어도 결과가 흔들리지 않는다.

### 면접 질문 포인트

- 동시성 테스트에서 `CountDownLatch`를 사용한 이유는 무엇인가?
- 단순 반복 테스트와 동시성 테스트의 차이는 무엇인가?
- 테스트에서 성공 수와 DB count를 둘 다 검증한 이유는 무엇인가?

---

## 10.3 부하 테스트 결과 분석 문서

### 목적

정합성은 해결했지만 비관적 락 구조가 부하에 취약하다는 점을 분석한다.

### 구현 범위

- 부하 테스트 조건 정리
- 장애 현상 정리
- row lock 병목 원인 분석
- Redis/Kafka 기반 개선 방향 제안

### 완료 기준

- 정합성 테스트는 통과했지만 부하 테스트에서 실패한 이유를 설명할 수 있다.
- 다음 개선 방향이 현재 병목과 연결되어 있다.

### 면접 질문 포인트

- 정합성과 성능 중 무엇을 우선했는가?
- 비관적 락이 서버 장애로 이어질 수 있는 이유는 무엇인가?
- 선착순 쿠폰 발급을 대규모 트래픽에서 어떻게 설계할 것인가?
- Redis/Kafka를 쓴다면 어떤 기준으로 선택할 것인가?

---

## 11. 포트폴리오 글 구성

## 제목 후보

**선착순 쿠폰 발급에서 DB 락으로 정합성을 보장하고, 부하 테스트로 한계를 확인한 과정**

## 글 흐름

1. 선착순 쿠폰 발급에서 왜 동시성 제어가 필요한지 설명
2. 재고 초과 발급 문제 재현
3. 동일 사용자 중복 발급 문제 재현
4. DB atomic update 적용
5. unique constraint 적용
6. pessimistic lock 적용
7. 동시성 테스트 통과 결과 정리
8. 부하 테스트에서 발견한 row lock 병목 설명
9. queue 기반 구조로 개선 방향 제안

---

## 12. 면접 대비 Q&A

## Q1. 왜 애플리케이션에서 중복 체크하면 안 되나요?

동시 요청에서는 여러 트랜잭션이 동시에 “아직 발급 이력이 없다”고 판단할 수 있기 때문이다.

따라서 애플리케이션 체크는 빠른 실패 처리 용도로는 사용할 수 있지만, 최종 정합성은 DB unique constraint로 보장해야 한다.

---

## Q2. atomic update와 pessimistic lock의 차이는 무엇인가요?

atomic update는 조건을 만족할 때만 DB가 한 번에 값을 변경하는 방식이다.

수량 초과 방지에는 효과적이지만, 발급 이력 저장 같은 다른 작업과 함께 순차성을 보장하지는 않는다.

pessimistic lock은 row를 잠가 다른 트랜잭션이 동시에 수정하지 못하게 한다.

정합성은 강하지만 락 대기와 처리량 저하 문제가 있다.

---

## Q3. 왜 비관적 락을 선택했나요?

이번 문제의 우선순위는 성능보다 정합성이었다.

선착순 쿠폰 발급에서 재고 초과 발급이나 중복 발급은 비즈니스적으로 치명적이다.

따라서 먼저 DB row lock으로 순차 처리를 보장해 정합성 문제를 해결했다.

---

## Q4. 비관적 락의 단점은 무엇인가요?

특정 쿠폰에 요청이 몰리면 모든 요청이 같은 row lock을 기다리게 된다.

그 결과 트랜잭션 시간이 길어지고 DB connection을 오래 점유한다.

부하가 커지면 connection pool 고갈, timeout, 서버 장애로 이어질 수 있다.

---

## Q5. 부하 테스트에서 서버가 터진 이유를 어떻게 설명할 수 있나요?

정합성을 위해 쿠폰 row 하나에 비관적 락을 걸었기 때문이다.

동시 요청이 많아질수록 요청들이 병렬 처리되지 못하고 락 앞에서 대기한다.

이 대기 시간이 누적되면서 DB connection과 서버 thread가 고갈되어 장애가 발생한 것으로 볼 수 있다.

---

## Q6. 다음에는 어떻게 개선할 건가요?

쿠폰 발급 요청을 API 서버에서 직접 DB row lock으로 처리하지 않고 queue 기반으로 분리할 것이다.

예를 들어 Kafka를 사용한다면 `couponId`를 partition key로 사용해 같은 쿠폰 요청은 순서를 보장하고, 다른 쿠폰 요청은 병렬 처리할 수 있다.

또는 Redis atomic counter로 선착순 수량을 빠르게 차감하고, DB 저장은 비동기로 처리하는 방식도 검토할 수 있다.

---

## 13. 이번 주 최소 산출물

## 13.1 동시성 문제 해결 보고서

### 파일명

```text
coupon-concurrency-control.md
```

### 포함 내용

- 문제 상황
- atomic update 시도
- unique constraint 적용
- pessimistic lock 적용
- 동시성 테스트 결과
- 부하 테스트 한계
- 다음 개선 방향

### 완료 기준

- 코드 캡처 없이도 문제와 해결 흐름을 설명할 수 있다.
- 면접 질문 5개 이상에 답변할 수 있다.

---

## 13.2 코드 정리

현재 발급 로직은 다음 두 경로로 나뉘어 있다.

```text
IssuedCouponService.issueCoupon()
CouponService.issue()
```

산출물 완성도를 높이려면 최종 발급 API가 하나의 경로만 사용하도록 정리하는 것이 좋다.

### 완료 기준

- 실제 Controller가 호출하는 발급 로직이 명확하다.
- 동시성 테스트도 최종 발급 로직을 호출한다.
- unique constraint가 엔티티 또는 migration에 명시되어 있다.

---

## 13.3 부하 테스트 분석표

| 항목 | 내용 |
|---|---|
| 테스트 목적 | 비관적 락 기반 발급 API의 부하 한계 확인 |
| 요청 패턴 | 동일 couponId에 동시 요청 집중 |
| 성공한 점 | 재고 초과, 중복 발급 방지 |
| 실패한 점 | 서버 장애 또는 timeout 발생 |
| 원인 | coupon row lock 병목, DB connection 점유 |
| 개선 방향 | Redis/Kafka 기반 대기열 구조 |

---

## 최종 요약

DB atomic update, unique constraint, pessimistic lock을 단계적으로 적용해 쿠폰 발급 정합성은 확보했다.

하지만 부하 테스트에서 row lock 기반 구조의 처리량 한계를 확인했다.

따라서 다음 개선 단계는 DB 락 기반 즉시 처리 구조에서 Redis 또는 Kafka 기반 queue 처리 구조로 전환하는 것이다.
