server:
  port: 8080

spring:
  application:
    name: coupon-system
  datasource:
    url: jdbc:mysql://localhost:13306/coupon_db
    username: coupon
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
