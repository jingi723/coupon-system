spring:
  application:
    name: coupon-system
  datasource:
    url: jdbc:mysql://localhost:3306/coupon_db
    username: root
    password: rlawlsrl723
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true